import SectionTitle from "../SectionTitle/SectionTitle";


const Unknow = () => {
    return (
        <div className="bg-black">
            <SectionTitle heading={"asmdfsa"}/>
        </div>
    );
};

export default Unknow;